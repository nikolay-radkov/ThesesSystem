@using Microsoft.AspNet.Identity
@using ThesesSystem.Web.Infrastructure.Constants
@if (this.User.IsInRole(GlobalConstants.ADMIN) || this.User.IsInRole(GlobalConstants.TEACHER))
{
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Administration<span class="caret"></span></a>
        <ul class="dropdown-menu" role="menu">
            <li>@Html.ActionLink("Continents", "Index", "Continent", new { area = "Administration" }, new { @class = string.Empty })
            <li>
            <li>@Html.ActionLink("Countries", "Index", "Country", new { area = "Administration" }, new { @class = string.Empty })</li>
            <li>@Html.ActionLink("Cities", "Index", "City", new { area = "Administration" }, new { @class = string.Empty })</li>
            <li class="divider"></li>
            <li>@Html.ActionLink("Feedbacks", "Index", "Feedback", new { area = "Administration" }, new { @class = string.Empty })</li>
        </ul>
    </li>
}